# Core dependencies for the API server
onnxruntime>=1.22.0
numpy>=1.21.0
requests>=2.25.0

# Optional: For advanced features
# transformers>=4.20.0  # Only if you want to use the full HuggingFace model
# torch>=1.12.0         # Only if you want to use PyTorch model
# fastapi>=0.68.0       # Only if you want to use FastAPI instead of simple HTTP server
# uvicorn>=0.15.0       # Only if you want to use FastAPI